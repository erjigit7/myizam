# Eval-отчёт: golden set 30 вопросов (ТЗ v2.0 §11)

## ОБНОВЛЕНИЕ 17.07.2026: собственный кыргызский переводчик (kazllm-legal-translate)

Мост и перевод ответов для ky переведены с qwen на дообученный KyrgyzLLM
(QLoRA поверх KazLLM-8B, 30.2k примеров из параллельного корпуса 8 кодексов,
1 эпоха, train loss 0.244 / eval loss 0.411). Итоги того же golden set:

| Метрика | qwen-мост (15.07) | **kazllm-translate (17.07)** |
|---|---|---|
| hit@5 общий | 89% | **96%** (26/27) |
| hit@5 кыргызский | 40% | **80%** (4/5) |
| citation accuracy | 78% | **89%** |
| trap refusal | 3/3 | **3/3** |

similarity кыргызских вопросов поднялась с 0.41–0.60 до 0.57–0.74 — переводы
моста стали эквивалентны русским формулировкам. Ответы пользователю теперь
на связном юридическом кыргызском с сохранёнными маркерами [N].

**Найденное и ЗАКРЫТОЕ ограничение переводчика:** на живом демо v1 исказил
дробь словами («одна четверть» → «төрттөн үчү», т.е. три четверти) при верном
русском оригинале — ровно слепая зона guard-детектора (числа словами).
Причина: во всём параллельном корпусе лишь ~22 пары с настоящими дробями.
Фикс (v2, 18.07): прицельная аугментация — реальные пары с дробями размножены
синхронной подстановкой по проверенной таблице соответствий (242 уникальных
×20 + 3k оригинальных пар против забывания), короткий continue-проход
(lr 2e-5, 1 эпоха). Результат: «одна четверть» → «төрттөн бири» ✓, модель
различает ¼ (төрттөн бири) и ¾ (төрттөн үчү) ✓, eval loss не деградировал
(0.422 против 0.411) — живой ответ про алименты стал фактически точным.
Для guard-детектора та же аугментация остаётся в плане (фаза 2).

---

## ОБНОВЛЕНИЕ 19.07.2026: guard v2-legal — слепая зона чисел словами закрыта

ragguard-kyrgyz дообучен на юрдомен (RagGuard/legal/): 4836 пар из статей
кодексов (ru+ky), три типа порчи — цифры, числа/дроби СЛОВАМИ (новое), чужое
утверждение; +весь исходный новостной датасет против забывания. Held-out
юр-тест (по типам порчи):

| Тип порчи | v1 | v2-legal |
|---|---|---|
| Числа словами | 0.51 (монетка; испорченным даёт avg 0.98) | **0.95** (испорченные avg 0.05) |
| Цифры | 0.63 | **0.91** |
| Чужое утверждение | 0.66 | **0.99** |
| Новостной тест (регрессия) | 0.979 | 0.983 — нет |

Живой sanity (7 рукописных пар): кейс алиментов («одной четверти»→«трех
четвертей») теперь ловится с Δ+0.99; все цифровые — уверенно. Новая грань:
на двух парах честный ПЕРЕФРАЗ статьи тоже получает низкий скор — модель
стала строгой к неточному цитированию (обучалась на «честный = дословное
предложение статьи»). Для shadow это безопасная сторона ошибки; для warn —
предмет калибровки порога и следующей итерации данных (честные перефразы
как позитивы). Сервируется: ml-sidecar, GUARD_KG_MODEL=ragguard-kyrgyz-v2-legal.

---

## Прогон 15.07 (qwen-мост) — исходные данные ниже

**Дата:** 15.07.2026 · **Стенд:** локальный (pgvector + Ollama bge-m3 + sidecar CPU)
· **LLM:** qwen2.5:7b-instruct (dev-режим; прод — gpt-4o-mini, см. caveat ниже)
· **MIN_SIMILARITY:** 0.5 · Скрипт: `eval/run_eval.py`, данные: `eval/golden_set.json`, детали: `eval/last_run.json`

## Итог по гейту фазы 4

| Метрика | Definition of Done | Результат | |
|---|---|---|---|
| Retrieval hit@5 (после rerank) | ≥ 80% | **89%** (24/27) | ✅ |
| Trap refusal | 3/3 | **3/3** | ✅ |
| Citation accuracy | ≥ 75% | 78% | ✅ |
| Guard sanity (подмена цифры) | испорченный ≪ честного | Δ = +0.99 | ✅ |

## Retrieval: до и после rerank

| Срез | hit@20 (до rerank) | hit@5 (после rerank) |
|---|---|---|
| Все (27) | 89% | 89% |
| Русские (20) | **100%** | **100%** |
| Английские (2) | 100% | 100% |
| Кыргызские (5) | 40% | 40% |

- На этом корпусе bge-m3 сразу ставит нужную статью высоко (в фазе 1 гейт 10/10 был top-1),
  поэтому rerank не меняет hit-rate — его вклад в *порядок внутри* top-5 (лучший фрагмент
  первым уходит в промпт). Ожидаемый прирост из marketplace-ai-assistant (76→88%)
  здесь не воспроизводится, потому что retrieval уже на потолке. Честно фиксируем.
- **Кыргызский 40% — это qwen-мост, не поиск**: ky-вопросы переводятся на русский
  локальной моделью криво (для gs-024 мост дал битый JSON — добавлен regex-фолбэк),
  similarity падает до 0.41–0.60. Русские же формулировки тех же вопросов дают 100%.
  Перепрогнать ky-срез после переключения на gpt-4o-mini.

## Калибровка MIN_SIMILARITY

Распределения similarity (max по top-20):

- честные ru/en-вопросы: **0.60–0.76** (min 0.604)
- ловушки: 0.434, 0.498, **0.597**
- честные ky (через кривой мост): 0.41–0.60 — пересекаются с ловушками

Вывод: **жёсткого порога, отделяющего ловушки от честных, не существует**
(0.597 против 0.604 — бритвенный зазор, а ky-вопросы ниже ловушек). Рабочая
конфигурация: **MIN_SIMILARITY = 0.5** (отсекает явный мусор: 2 из 3 ловушек)
**+ LLM-отказ по правилу 3 промпта** (фраза «нет ответа» без маркеров → refused) —
вместе дают 3/3 ловушки при сохранении ответов на все честные вопросы.
Перекалибровать на живых логах после деплоя.

## Guard (shadow-режим)

Распределение по 23 неотказным ответам: p50 = 0.995, avg = 0.907, min = 0.003
(низкие значения — на кыргызских ответах qwen с ломаной грамматикой; это shadow, на ответ не влияет).

### Sanity: подмена фактов руками (`eval/guard_sanity.py`)

| Тип порчи | Пример | честный | испорченный | Δ |
|---|---|---|---|---|
| Цифра («28»→«45 дней») | ТК ст.68 | 0.996 | **0.008** | +0.99 |
| Цифра («70 и 56»→«30 и 14») | ТК ст.148 | 0.996 | **0.010** | +0.99 |
| Цифра («2 часа»→«6 часов») | ТК ст.108 | 0.996 | **0.006** | +0.99 |
| Число словом («трех»→«девяти месяцев») | ТК ст.24 | 0.996 | 0.996 | ~0 |
| Число словом («одной четверти»→«трех четвертей») | СК ст.86 | 0.996 | 0.996 | ~0 |
| Единица («в месяц»→«в квартал») | ТК ст.94 | 0.996 | 0.996 | ~0 |
| Цифра в ответе при числе-слове в контексте | ЖК ст.45 | **0.009** (ложная тревога) | 0.010 | ~0 |

**Главная находка:** ragguard-kg на юридическом домене работает как детектор
*цифровой консистентности* — цифра в ответе обязана буквально встречаться в контексте.
Подмену цифр ловит идеально (Δ+0.99), словесные числа и единицы измерения — не видит,
а честный ответ цифрой при числе-словом в законе даёт ложную тревогу. Прямое следствие
синтетических негативов датасета (программная подмена цифр) — ровно тот доменный сдвиг,
который заявлен в caveat ragguard. Это и есть обоснование shadow-режима на старте
и план дообучения на юр.домене (фаза 2): негативы со словесными числами + пары
«цифра↔словесное число».

## Латентность (локальный стенд, qwen на GPU)

- полный ответ: avg 4.7 с; отказ (без генерации/до неё): avg 1.2 с; кэш-повтор: 0.03 с.

## Caveats

1. Все LLM-зависимые метрики (citation, ky-качество, guard-скоры кыргызских ответов)
   получены на qwen2.5-7B — прод-модель gpt-4o-mini даст другой (лучший) профиль,
   особенно на кыргызском. Retrieval-метрики от LLM не зависят (кроме ky-моста).
2. Кыргызский текст qwen генерирует с зацикливаниями — ky-ответы в проде на qwen
   недопустимы, это dev-ограничение.
